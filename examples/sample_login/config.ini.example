; =====================================================
; このファイルを config.ini にコピーして値を書き換えてください。
; セクション名・キー名は大文字で書く（Python 側の config.SECTION.KEY と一致する）
; 認証情報・個人情報はここに書かず python -m comken.credentials で登録する
; =====================================================

[REPORT]
OUTPUT_FOLDER = C:\Users\Public\Downloads
