; 社内BO環境（オフライン）では pip install -r は使えない。参照・記録用。
; --- 必須 ---
openpyxl
selenium
pywin32
; --- 任意（導入承認が下りた場合のみ）---
; requests        … salesforce（SalesforceApiClient。OAuth クライアントクレデンシャル）を使う場合のみ
; pypdf           … comken.pdf（PDF の結合・分割・テキスト抽出）を使う場合のみ
; --- 開発用 ---
ruff
pytest
