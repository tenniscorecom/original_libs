# このプロジェクトが使う外部ライブラリ（参照・記録用）。
# ※ オフラインの社内BO環境では pip install は使えない。comken は共有サーバーを直接参照する。
#    ネット接続がある開発環境で仮想環境を作るときに使う。

comken
openpyxl
pywin32

# 使う機能に応じて追加する（使わないものは消す）
# selenium            # ブラウザ操作
# requests            # Salesforce（REST / api クライアント）
# simple-salesforce   # Salesforce（simple_sf / bulk クライアント）
# pypdf               # PDF の結合・分割・抽出
